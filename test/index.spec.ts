// we will import using the require spec so that we do no worry about factory functionality

import { describe } from 'mocha';

describe('application', () => {
    it('should simply pass', () => {})
});
